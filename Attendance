import java.util.Scanner;

public class AttendanceCalculator { public static void main(String[] args) { Scanner scanner = new Scanner(System.in);

System.out.print("Enter total classes: ");
    int totalClasses = scanner.nextInt();
    
    System.out.print("Enter attended classes: ");
    int attendedClasses = scanner.nextInt();
    
    double attendancePercentage = ((double) attendedClasses / totalClasses) * 100;
    
    System.out.printf("Attendance Percentage: %.2f%%\n", attendancePercentage);
    
    scanner.close();
}

}

